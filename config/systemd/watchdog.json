{
  "watchdog": {
    "enabled": true,
    "interval_ms": 10000,
    "timeout_sec": 30,
    "auto_start": true,
    "description": "Watchdog configuration for RT process",
    "notes": [
      "interval_ms should be WatchdogSec/3 or less for safety margin",
      "WatchdogSec is configured in systemd service file (30s)",
      "Notifications sent every 10s provide 3x safety margin"
    ]
  },
  "restart_policy": {
    "on_failure": true,
    "restart_sec": 5,
    "start_limit_burst": 5,
    "start_limit_interval_sec": 60
  }
}
